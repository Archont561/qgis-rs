[package]
name        = "qgis-render"
description = "Backend-agnostic rendering engine for qgis-rs: projects, extents, CRS, tiles"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde     = { workspace = true }
thiserror = { workspace = true }
